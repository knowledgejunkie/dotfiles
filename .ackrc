# General Settings
# ################

# only search with case sensitivity if there is mixed case
--smart-case

# follow symlinks
--follow

# always sort the files
--sort-files

# always color, even if piping to a another program
--color

# no grouping
--noheading
--break

# always use a PAGER
--pager=less

# always print filename prefix
--with-filename

# General ignore directories
--ignore-dir=log
--ignore-dir=tmp
--ignore-dir=vendor
--ignore-dir=build/html
--ignore-dir=docs/build
--ignore-dir=migrations


# Updated Filetypes
###################

--type-add=css:ext:sass,less,scss
--type-add=js:ext:coffee
--type-add=xml:ext:xsd


# Custom Filetypes
# ################

--type-set=coffee:ext:coffee
--type-set=cucumber:ext:feature
--type-set=linguist:ext:ts
--type-set=web:ext:html,htm,js,css
